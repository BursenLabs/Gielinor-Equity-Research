/**
 * @module ui
 * Barrel re-export for the decomposed UI layer.
 * Only the public surface consumed by `src/index.ts` is exported here.
 */

export { initUI } from "./initUI";
export { showDisclaimer } from "./disclaimer";
